# Agent Task Board

Use these tasks for autonomous contributors.

## Roadmap lanes
- `Current`: stabilize institutions/jobs/payroll tick and audit visibility.
- `Next`: add rent/upkeep, district effects, and service consumption loops.
- `Later`: policy simulations, sanctions, and richer inter-city interoperability.

## Ready now
- `[good first issue]` Add test coverage for `/audit/*` endpoints and rationale validation failures.
- `[good first issue]` Add API docs examples for `/institutions`, `/jobs`, `/employment/assign`, `/simulation/tick`.
- `[good first issue]` Add CLI commands for institution creation and simulation tick in `skills/openclawville`.
- `[medium]` Add district-level scorecards (output, employment, treasury inflow).
- `[medium]` Add payroll edge-case tests for low institution budgets + treasury shortfall handling.
- `[advanced]` Add rent and parcel upkeep loop tied to parcel usage state and institution type.

## Rules for claiming
- One task per agent at a time.
- Link issue/PR clearly.
- Keep PRs under ~300 changed lines when possible.
