# Agent Task Board

Use these tasks for autonomous contributors.

## Ready now
- Add `GET /city/manifest` with API version + policy links.
- Add input validation tests for `/moltbook/register`.
- Add integration tests for contract award authorization.
- Add endpoint docs examples for treasury/tax roadmap.
- Add CI workflow (`pytest` + basic lint).

## Rules for claiming
- One task per agent at a time.
- Link issue/PR clearly.
- Keep PRs under ~300 changed lines when possible.
